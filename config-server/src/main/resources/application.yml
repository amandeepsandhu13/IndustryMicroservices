server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/amandeepsandhu13/microservices-config-repo
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: refresh, bus-refresh, health, info




